'use client'import { useEffect, useState, useCallback } from 'react'import { createClient } from '@/lib/supabase/client'import { Database } from '@/types/supabase'type CreditAmount = Database['public']['Tables']['credit_amount']['Row']export default function CreditAmountPage() {  const [credits, setCredits] = useState<CreditAmount[]>([])  const [loading, setLoading] = useState(true)  const [selectedApp, setSelectedApp] = useState<string>('all')  const [timeRange, setTimeRange] = useState<'today' | 'week' | 'month' | 'all'>('all')  const [apps, setApps] = useState<string[]>([])  const supabase = createClient()  const loadCredits = useCallback(async () => {    const query = supabase      .from('credit_amount')      .select('*')      .eq('extraction_status', 'success')      .order('timestamp', { ascending: false })      .limit(100)    const { data } = await query    if (data) setCredits(data)    setLoading(false)  }, [supabase])  const loadAppList = useCallback(async () => {    const { data } = await supabase      .from('credit_amount')      .select('app_name')      .eq('extraction_status', 'success')      .not('app_name', 'is', null) as { data: { app_name: string | null }[] | null }    if (data) {      const uniqueApps = Array.from(new Set(data.map(d => d.app_name).filter(Boolean))) as string[]      setApps(uniqueApps.sort())    }  }, [supabase])  useEffect(() => {    loadCredits()    loadAppList()    const channel = supabase      .channel('credit-amount-updates')      .on(        'postgres_changes',        {          event: 'INSERT',          schema: 'public',          table: 'credit_amount',          filter: 'extraction_status=eq.success'        },        (payload) => {          const newCredit = payload.new as CreditAmount          setCredits(prev => [newCredit, ...prev])        }      )      .subscribe()    return () => {      supabase.removeChannel(channel)    }  }, [supabase, loadCredits, loadAppList])  const filteredCredits = credits.filter(credit => {    if (selectedApp !== 'all' && credit.app_name !== selectedApp) {      return false    }    if (timeRange !== 'all') {      const creditDate = new Date(credit.timestamp)      const now = new Date()      if (timeRange === 'today') {        const today = new Date()        today.setHours(0, 0, 0, 0)        if (creditDate < today) return false      } else if (timeRange === 'week') {        const weekAgo = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000)        if (creditDate < weekAgo) return false      } else if (timeRange === 'month') {        const monthAgo = new Date()        monthAgo.setMonth(monthAgo.getMonth() - 1)        if (creditDate < monthAgo) return false      }    }    return true  })  const totalAmount = filteredCredits.reduce(    (sum, c) => sum + (c.amount || 0),     0  )  if (loading) {    return (      <div className="flex items-center justify-center h-64">        <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-red-600"></div>      </div>    )  }  return (    <div className="space-y-6">      {}      <div className="bg-white dark:bg-gray-800 p-4 rounded-lg shadow">        <div className="flex flex-wrap gap-4">          {}          <div className="flex-1 min-w-[200px]">            <label className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">              Lọc theo App            </label>            <select              value={selectedApp}              onChange={(e) => setSelectedApp(e.target.value)}              className="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white"            >              <option value="all">Tất cả ứng dụng</option>              {apps.map(app => (                <option key={app} value={app}>{app}</option>              ))}            </select>          </div>          {}          <div className="flex-1 min-w-[200px]">            <label className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">              Lọc theo thời gian            </label>            <select              value={timeRange}              onChange={(e) => setTimeRange(e.target.value as 'today' | 'week' | 'month' | 'all')}              className="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-white"            >              <option value="all">Tất cả</option>              <option value="today">Hôm nay</option>              <option value="week">7 ngày qua</option>              <option value="month">30 ngày qua</option>            </select>          </div>        </div>        {}        <div className="mt-4 pt-4 border-t border-gray-200 dark:border-gray-700">          <div className="flex justify-between items-center">            <span className="text-gray-600 dark:text-gray-400">              Tổng cộng ({filteredCredits.length} giao dịch):            </span>            <span className="text-2xl font-bold text-red-600 dark:text-red-400">              -{formatMoney(totalAmount)}            </span>          </div>        </div>      </div>      {}      {}      {}      <div className="bg-white dark:bg-gray-800 p-6 rounded-lg shadow">        <h3 className="text-lg font-semibold text-gray-900 dark:text-white mb-4">          📋 Chi tiết giao dịch        </h3>        {filteredCredits.length === 0 ? (          <div className="text-center py-12">            <p className="text-gray-500 dark:text-gray-400">              Không có giao dịch nào            </p>          </div>        ) : (          <div className="space-y-3">            {filteredCredits.map((credit) => (              <div                key={credit.id}                className="border border-gray-200 dark:border-gray-700 p-4 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition"              >                <div className="flex justify-between items-start gap-4">                  <div className="flex-1 min-w-0">                    <h4 className="font-semibold text-gray-900 dark:text-white mb-1">                      {credit.title}                    </h4>                    <p className="text-sm text-gray-600 dark:text-gray-300 mb-2 line-clamp-2">                      {credit.body}                    </p>                    <div className="flex flex-wrap gap-3 text-xs text-gray-500 dark:text-gray-400">                      <span className="flex items-center gap-1">                        📱 {credit.app_name || credit.package_name}                      </span>                      <span className="flex items-center gap-1">                        🕐 {new Date(credit.timestamp).toLocaleString('vi-VN')}                      </span>                    </div>                  </div>                  <div className="text-right">                    <div className="text-2xl font-bold text-red-600 dark:text-red-400">                      -{formatMoney(credit.amount || 0)}                    </div>                    <div className="text-xs text-gray-500 dark:text-gray-400 mt-1">                      {credit.currency || 'VND'}                    </div>                  </div>                </div>              </div>            ))}          </div>        )}      </div>    </div>  )}function formatMoney(amount: number): string {  return new Intl.NumberFormat('vi-VN', {    style: 'currency',    currency: 'VND',    minimumFractionDigits: 0,    maximumFractionDigits: 0  }).format(amount)}